# Dependencies
node_modules/

# Build output
dist/
build/

# Superseded server-era code, kept locally for reference only
legacy/

# Local working notes, not for publication
Supporting Docs/

# Environment
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
logs/

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
._*
Thumbs.db
ehthumbs.db

# Test coverage
coverage/

# Temporary files
tmp/
temp/

# Crawler lock and temp files
nexus/.lock
modio/.lock
*.tmp

# Working notes, deliberately not published. This is the live worklist: it
# carries half-finished measurements, wrong figures caught mid-correction and
# blunt notes to self, none of which belong in a public repository. It was
# committed once by accident on 2026-08-14 by a `git add docs` that swept the
# directory, which is why it is named here rather than left to discipline.
docs/outstanding.md

# One machine's reading history for scripts/watch-community.mjs, not project data.
scripts/community-state.json

# Findings from reading every mod description. Analysis notes, not project
# data, and the cache they are computed from is not in the repository either.
